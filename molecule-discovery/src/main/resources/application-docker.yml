spring:
  profiles:
    include:
      - show-sql
  datasource:
      url: jdbc:mysql://application-mysql:3306/application
      username: user
      password: user
  jpa:
    hibernate:
      ddl-auto: update
  cloud:
    stream:
      binders:
        default:
          type: rabbit
          environment:
            spring:
              rabbitmq:
                host: rabbitmq
                port: 5672
                username: user
                password: user
                virtual-host: /